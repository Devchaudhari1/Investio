services:
  backend:
    image: investio_backend:latest
    container_name: backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    image: investio_frontend:latest
    container_name: frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend